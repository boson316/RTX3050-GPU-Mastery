# Generated diagrams

These PNGs are generated by:

```bash
python docs/scripts/generate_diagrams.py
```

(from the repository root)

| File | Description |
|------|-------------|
| **gpu_memory_hierarchy.png** | GPU memory hierarchy: global memory → L2 cache → shared memory → registers (with approximate latency/bandwidth/scope) |
| **cuda_thread_hierarchy.png** | CUDA thread hierarchy: grid → block → warp → thread (nested execution units) |
| **flashattention_tiling.png** | FlashAttention tiling: Q/K/V split into BLOCK_M and BLOCK_N row blocks; blocks loaded into SRAM; online softmax; no full S×S matrix in global memory |

See also:

- [Memory hierarchy diagrams](../memory_hierarchy_diagrams.md)
- [GPU memory hierarchy](../gpu_memory_hierarchy.md)
- [FlashAttention algorithm](../flashattention_algorithm.md)
